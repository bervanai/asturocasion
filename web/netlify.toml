[build]
  command = "npm install --legacy-peer-deps && npm install serverless-http && npx vite build && node scripts/generate-seo.mjs"
  publish = "dist/public"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true

# Legacy WordPress URLs (still indexed by Google) → new catalogue, 301 so link
# equity transfers to the live pages and the old thin/duplicate URLs drop out
# of the index instead of turning into 404s/soft-404s.
[[redirects]]
  from = "/coches-de-segunda-mano-y-ocasion/*"
  to = "/catalogo"
  status = 301
  force = true

[[redirects]]
  from = "/make/*"
  to = "/catalogo"
  status = 301
  force = true

[[redirects]]
  from = "/model/*"
  to = "/catalogo"
  status = 301
  force = true

[[redirects]]
  from = "/category/*"
  to = "/catalogo"
  status = 301
  force = true

[[redirects]]
  from = "/tag/*"
  to = "/catalogo"
  status = 301
  force = true

# SPA fallback. Not forced, so physical files generated by scripts/generate-seo.mjs
# (per-route index.html) are served first; everything else falls back to the app.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
